name: setup nodejs
on: workflow_dispatch
jobs:
  setup_nodejs:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v5
      with:
        node-version: 18
    - run: npm ci
    - run: npm test
    
    
    
